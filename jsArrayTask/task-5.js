// question no 5:

// 5. Combining Arrays
// Instructions:

// Create two arrays of your choice.
// Use the concat method to combine the two arrays into a new array.
// Print both the original arrays and the combined array using console.log().

let fruits = ['mango', 'banana', 'apple', 'jackfruit', 'orrange'];
let book = ['Physics', 'Chemistry', 'Biology', 'Javascript', 'Math', 'English', 'Bangla'];

let fruBok = fruits.concat(book);

console.log(fruBok);